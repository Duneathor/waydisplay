# CMake/build outputs
/build*/
cmake-build-*/
CMakeFiles/
CMakeCache.txt
compile_commands.json
cmake_install.cmake

# Binaries and libraries
/waydisplay-client
/waydisplay-server
/waydisplay_proto
*.a
*.so
*.o

# Editor/OS noise
*.swp
*~
.DS_Store

# Arch makepkg outputs when building from repository root.
# Do not ignore /src/: it contains the project's tracked source, and doing
# so hides newly added source files from git add and status.
/src/build-*/
/pkg/
/*.pkg.tar.*
/packaging/arch/waydisplay-src.tar.gz
